node_modules/
build/
*.log
.DS_Store

# Local agent tooling and skill-manager artifacts
.claude/
.agents/
skills-lock.json

# Local secrets, never commit (the template is tracked deliberately)
.env
.env.*
!.env.example
